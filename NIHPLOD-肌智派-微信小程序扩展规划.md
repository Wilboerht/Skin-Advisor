# NIHPLOD 肌智派 · 微信小程序扩展项目规划

> 版本：v1.0（2026-08-28）
> 状态：待启动
> 前置分析结论：服务端零重写可行，核心风险（人脸检测）已通过微信原生 VisionKit 化解，MVP 工期 5–7 周。

---

## 1. 背景与目标

现有「肌智派 AI 测肤」为 Next.js H5 应用（本仓库），承担 SEO 与微信外分享落地。为承接微信生态流量、打通"测肤 → 报告 → 商城购买"闭环，将测肤能力扩展至微信小程序。

**目标：**

- 在小程序内完成完整测肤流程：问卷 → 扫脸（4 角度自动拍摄）→ AI 分析 → 报告 → 产品推荐。
- 小程序用户与 H5 用户账号互通（手机号 / unionid），报告历史互相可见。
- H5 站点保留不动，继续承担 SEO 与外部分享落地。

**非目标（本期不做）：**

- 活动运营模块（已于 2026-08 下线，前端为静态说明，不移植）。
- 独立小程序冷启动（推荐做进商城小程序，见 §3）。
- tfjs-wechat 端上深度学习方案（已被 VisionKit 方案取代，见 §5.1）。

---

## 2. 现状资产盘点（可复用清单）

### 2.1 服务端 API（全部可直接复用，零重写）

| API | 用途 | 小程序侧对应 |
|---|---|---|
| `GET /api/advisor/questions` | 问卷题目（DB 优先，静态降级） | 问卷页数据 |
| `GET /api/advisor/check-config` | AI 配置与排队状态检查 | 入口预检 |
| `GET /api/advisor/test-limit` | 测试次数预检 | 入口预检（identity 换 openid） |
| `POST /api/advisor/face-analyze` | 面部分析（OpenAI 视觉） | 拍摄完成后调用 |
| `POST /api/advisor/analyze` | 肌肤分析（异步会话） | 分析中页轮询 |
| `GET /api/advisor/session/status` | 分析状态轮询 | 分析中页 |
| `POST /api/oss/sign` | OSS 上传签名 | 照片上传 |
| `POST /api/advisor/analytics/track` | 埋点 | 全链路埋点复用 |
| `POST /api/advisor/kf-link` | 企业微信客服链接 | 结果页咨询入口 |
| `GET /api/products` | 产品列表 | 结果页推荐 |
| `POST /api/auth/send-code` | 短信验证码 | 手机号绑定（如需） |
| `GET /api/internal/mp-skin` | 按手机号拉报告/趋势/历史 | **已存在**，商城小程序在用，参考其 x-internal-key 模式 |

### 2.2 纯 TS 逻辑层（可搬运）

- `src/config/questions.ts`：问卷题目配置（纯数据，含 dependsOn 条件逻辑）。
- `src/lib/storage-keys.ts`：存储键约定（localStorage → `wx.setStorage` 映射）。
- zod 校验 schema、埋点参数结构（`useAdvisorAnalytics` 的事件定义）。
- `FaceCapture` 中的**角度状态机与阈值**（正脸/左转/右转/抬头的 yaw/pitch 判定逻辑）——检测数据源换成 VisionKit，判定逻辑照搬。

### 2.3 账号与微信生态

- 商城小程序已存在，通过内部接口读测肤报告，用户按 `User.phoneNumber` 打通。
- 已有认证服务号 / 开放平台主体：微信网页 OAuth（`/api/auth/wechat`）、公众号 webhook、模板消息、企业微信客服。
- **unionid 打通**：小程序与 H5 微信登录若挂同一开放平台主体，unionid 一致，账号可无缝合并。
- 域名已备案（沪ICP备2026014764号）、HTTPS 完备，小程序 request 合法域名直接添加。

### 2.4 资质合规

- 现有隐私协议与同意流程（`PrivacyConsent`）文案可复用，需做小程序版弹窗。
- 文案均为"护肤建议"向，不涉医疗宣称，符合类目要求。

---

## 3. 形态与技术选型（决策）

| 决策点 | 结论 | 理由 |
|---|---|---|
| 独立小程序 or 商城分包 | **商城小程序"AI 测肤"分包** | 用户体系已通；测完推荐商品直跳商城，转化闭环；避免冷启动 |
| 框架 | **Taro（React）分包** | 最大化复用 TS 逻辑层；团队现有 React 技术栈；`Taro.createVKSession` 有封装 |
| 人脸检测 | **VisionKit 为主 + 连拍选帧上云降级 + 服务端复核** | 见 §5.1 |
| 海报生成 | **服务端 sharp 合成** | H5 的 html-to-image 依赖 DOM 不可移植；服务端合成 H5/小程序共用一套 |
| H5 处理 | **保留不动** | SEO 与微信外分享落地不可替代 |

---

## 4. 页面清单与 API 映射

分包页面（约 7 个）：

| # | 页面 | 说明 | 主要 API |
|---|---|---|---|
| 1 | 测肤首页 | 入口、隐私协议弹窗 | `check-config`、`test-limit` |
| 2 | 性别选择 | 含游客态初始化 | — |
| 3 | 问卷页 | 单选/多选、条件题、自动切题、本地暂存 | `questions` |
| 4 | 扫脸引导 + 拍摄页 | 4 角度自动拍摄（核心页） | VisionKit、`oss/sign` |
| 5 | 分析中页 | 进度与轮询 | `face-analyze`、`analyze`、`session/status` |
| 6 | 结果页 | 报告、派系形象、产品推荐（跳商城商品页）、客服入口 | `products`、`kf-link` |
| 7 | 历史报告页 | 报告列表与详情 | 复用 `mp-skin` 或 advisor API |

---

## 5. 关键方案

### 5.1 人脸采集与自动拍摄（本项目最大风险点，已定方案）

**主方案：微信原生 VisionKit**

- `CameraContext.onCameraFrame` 取实时帧，节流 5–10 fps。
- `VKSession.detectFace`（基础库 2.25.0+，安卓微信 8.0.25+ / iOS 8.0.24+）返回 106 个 2D/3D 关键点。
- 由 3D 关键点计算 yaw/pitch/roll，套用从 H5 `FaceCapture` 移植的角度状态机：姿态到位且画面稳定 → 自动 `CameraContext.takePhoto`。
- 优势：系统级性能、零第三方依赖、零模型包体（现有 576K face-api 模型不再需要）、无 tfjs-wechat 社区库维护风险。

**降级方案（VK 不可用时自动切换，由 `session.start` 错误回调探测）：连拍选帧上云**

- 引导用户"缓慢左转 → 右转 → 抬头"4–5 秒，每秒采 2–3 帧 320px 小图（约 30KB/帧）批量上传。
- 服务端用 face-api node 版从连拍帧中自动选出每个角度最优帧。

**服务端统一复核（无论主/降级路径）**

- 新增 `POST /api/advisor/face-precheck`：对最终 4 张照片做质量复核（人脸检出、角度、清晰度），不通过则返回具体原因引导重拍。
- 实现选项：face-api node 版（自托管）或腾讯云人脸检测 API（直接返回 yaw/pitch/roll，4 张/次成本可忽略）。

**终极兜底**：纯手动拍摄 + 服务端校验重拍（流程不断，体验最弱，仅保底）。

### 5.2 登录与账号打通

1. 新增 `POST /api/auth/mp-login`：`wx.login` code → 换 openid（需配置小程序 appid/secret）→ 签发会话。
2. 手机号绑定：`getPhoneNumber` 按钮授权 → 并入现有 `User.phoneNumber` 体系（与商城、H5 三端合一）；同一开放平台主体下用 unionid 关联 H5 微信登录账号。
3. 游客体系：H5 的 cookieId + FingerprintJS 指纹在小程序替换为 openid，`test-limit` 限次逻辑复用。

### 5.3 海报生成

- 结果海报、派系形象海报改为**服务端 sharp 合成**（结果页已有 posterRef/html-to-image，迁移时统一收口）。
- 小程序端 `wx.saveImageToPhotosAlbum` 保存；分享卡片用 `onShareAppMessage` 配置。

### 5.4 合规与隐私

- 人脸照片属敏感个人信息：小程序版隐私协议弹窗（采集目的、存储周期、删除渠道），首次启动强制同意（对齐 H5 的入口守卫逻辑）。
- 摄像头权限用途声明；相册写入权限在使用时申请。
- 类目：美妆/工具；文案维持护肤建议口径。
- OSS 照片生命周期策略（现有 data-cleanup cron）覆盖小程序来源数据。

---

## 6. 分期计划与工期（1–2 人）

### 阶段 0：准备（约 1 周）

- 小程序 appid/secret 配置、request 合法域名、类目与隐私协议备案。
- `POST /api/auth/mp-login` 接口开发与联调。
- `POST /api/advisor/face-precheck` 服务端人脸复核接口。
- VisionKit 可用性 PoC（真机验证 detectFace 帧率与关键点质量）——**此 PoC 是全项目最早的 go/no-go 检查点**。

### 阶段 1：MVP（约 5–7 周）

| 周 | 内容 |
|---|---|
| W1–W3 | 问卷 + 性别选择 + 结果 + 历史报告（纯 API 对接与 UI） |
| W3–W4 | 相机采集 + VisionKit 自动拍摄 + OSS 上传 + 分析对接 |
| W5 | 海报（服务端 sharp）+ 分享 |
| W6 | 登录打通收尾 + 隐私合规 + 降级路径（连拍选帧） |
| W7 | 真机回归（覆盖 iOS/安卓高低端机）+ 提审 |

**MVP 验收标准：**

- 完整流程通过率：iOS/安卓各 3 款机型走通"问卷→拍摄→报告"。
- 自动拍摄：4 角度 VisionKit 引导到位判定准确率 ≥ 95%（人工抽检 100 次）。
- 降级路径：屏蔽 VK 后连拍选帧流程可完成。
- 账号：小程序完成的报告在 H5 历史报告中可见（手机号一致前提下）。
- 性能：中端安卓机（骁龙 7 系同级）拍摄页引导帧判定延迟 ≤ 200ms。

### 阶段 2：增强（另排期，2–3 周）

- 模板消息/订阅消息：报告完成通知、复测提醒（复用 wechat-lifecycle 模式）。
- 测肤数据与商城会员权益联动（积分/优惠券）。
- 拍摄体验打磨：实时引导动画、防抖与光线提示。
- 分享裂变：结果海报带小程序码（参数二维码）。

---

## 7. 风险清单与对策

| 风险 | 等级 | 对策 |
|---|---|---|
| VisionKit 真机表现不及预期（帧率/关键点漂移） | 中 | 阶段 0 PoC 先行；降级路径保底；角度阈值从 H5 移植后需真机调优 |
| 审核驳回（人脸敏感信息类目） | 中 | 隐私协议明示 + 非医疗口径文案；预留 1–2 轮提审时间 |
| 低端机拍摄页卡顿 | 中 | 帧节流、降低判定频率；必要时降级为手动拍 + 服务端校验 |
| 服务端复核增加分析延迟 | 低 | precheck 与分析并行/前置，node 版检测单帧 < 100ms |
| 小程序包体积超限 | 低 | 无模型文件；图片资产全部 CDN/OSS；分包加载 |
| H5 与小程序口径分叉 | 低 | 共用同一套服务端 API 与报告生成逻辑，UI 各自维护 |

---

## 8. 待决策事项（启动前确认）

- [ ] 商城小程序主体确认：是否与 H5 微信登录同一开放平台主体（决定 unionid 打通方式）。
- [ ] Taro 版本与分包结构：新建 Taro 工程嵌入商城，还是商城现有工程加分包。
- [ ] 服务端人脸复核选型：face-api node 版（自托管零边际成本）vs 腾讯云人脸检测（免维护按量付费）。
- [ ] 海报是否借迁移统一收口到服务端 sharp（影响 H5 现有海报逻辑）。
- [ ] 游客限次策略：openid 维度是否沿用现有每日次数规则。

---

## 附：H5 侧已知配合点（本仓库）

- 活动运营已下线，`/gift` 已 308 至首页弹窗（静态内容），小程序不做对应模块。
- `/api/internal/mp-skin` 已在服务商城小程序，新增 advisor 侧接口时沿用 `x-internal-key` + 双层限流模式。
- 预加载已全部空闲调度（`src/lib/idle.ts`），小程序无此概念但问卷页 API 数据量小，无需对应优化。
